- make OCCT and pythonOCC part of alidist so that everyone gets it automatically

- support multiple external detectors at the same time (needs refactor how we load geometries in buildGeometry)

- support to give sensitive action as ROOT macro for each external detector

- (maybe external detectors + actions can be read in via a composite JSON file)

- implement a BVHSurface solid as an exact representation of a CAD solid

- complete geometry configurable as JSON --> even the world volume ?

 